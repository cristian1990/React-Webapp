//Esto es la representacion donde se guardan todas las variables globales
//Cuando se carga por primera vez el estado inicial va a estar en blanco (vacio)
//La representacion de la tada que va a almacenar esta en formato json
export const initialState = {}; //Objeto vacio